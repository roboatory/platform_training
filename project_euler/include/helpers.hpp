#ifndef HELPERS_HPP
#define HELPERS_HPP

#include <vector>

int gcd(int a, int b);
int lcm(int a, int b);
bool is_palindrome(int original);
std::vector<int> prime_sieve(int limit);

#endif // HELPERS_HPP